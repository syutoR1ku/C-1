#include "Calclation.h"

// メイン関数
int main() {
    Calclation x, y;

    // インスタンスＸの処理
    x.SetA(5.0);
    x.SetB(10.0);
    x.Disp();

    // インスタンスＹの処理
    y.SetA(8.0);
    y.SetB(3.0);
    y.Disp();

    return 0;
}
